using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class nameOfCards : MonoBehaviour
{

    public string nameC;
    // Start is called before the first frame update
    void Start()
    {
        //parse the name of the card to an int
        
        
    }

    // Update is called once per frame
    void Update()
    {
       
        //get information of button pressed
        //Debug.Log(nameC);

        
    }
}
